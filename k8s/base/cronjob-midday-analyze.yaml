apiVersion: batch/v1
kind: CronJob
metadata:
  name: investmentology-midday-analyze
  namespace: investmentology
  labels:
    app.kubernetes.io/name: investmentology
    app.kubernetes.io/component: midday-analyze
spec:
  # Mon-Fri 16:30 UTC (12:30 ET) — midday re-analysis of watchlist
  schedule: "30 16 * * 1-5"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            app.kubernetes.io/name: investmentology
            app.kubernetes.io/component: midday-analyze
        spec:
          restartPolicy: OnFailure
          containers:
            - name: midday-analyze
              image: ghcr.io/charlieshreck/investmentology-api:latest
              command: ["python3", "-m", "investmentology", "cron", "daily-watchlist-analyze", "--limit", "10", "--min-hours", "6"]
              envFrom:
                - secretRef:
                    name: investmentology-secrets
              env:
                - name: QUANT_GATE_TOP_N
                  value: "100"
                - name: MIN_MARKET_CAP
                  value: "200000000"
              resources:
                requests:
                  memory: "512Mi"
                  cpu: "200m"
                limits:
                  memory: "1Gi"
                  cpu: "1000m"
