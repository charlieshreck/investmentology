FROM oven/bun:1 AS build

WORKDIR /app

COPY pwa/package.json pwa/bun.lockb* ./
RUN bun install --frozen-lockfile || bun install

COPY pwa/ .
RUN bun run build

FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html
COPY k8s/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
